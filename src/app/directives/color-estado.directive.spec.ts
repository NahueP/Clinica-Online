import { ColorEstadoDirective } from './color-estado.directive';

describe('ColorEstadoDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorEstadoDirective();
    expect(directive).toBeTruthy();
  });
});
